// See admin-login.dto.ts's comment — LogoutDto ({ refreshToken,
// allSessions? }) is equally generic and safe to reuse directly.
export { LogoutDto as AdminLogoutDto } from '../../auth/dto/logout.dto';
