// Route stub only -- do not build the Reset Password screen here.
// PR F4 replaces this file's contents with the real, Figma-derived screen.
import PlaceholderPage from "./PlaceholderPage";

export default function ResetPasswordPage() {
  return <PlaceholderPage title="Reset password" owner="PR F4" />;
}
