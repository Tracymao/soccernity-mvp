// Route stub only -- do not build the Profile screen here.
// PR F6 replaces this file's contents with the real, Figma-derived screen.
import PlaceholderPage from "./PlaceholderPage";

export default function ProfilePage() {
  return <PlaceholderPage title="Profile" owner="PR F6" />;
}
