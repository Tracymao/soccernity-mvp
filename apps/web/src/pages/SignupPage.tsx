// Route stub only -- do not build the age-gate/signup flow here.
// PR F3 replaces this file's contents with the real, Figma-derived screen(s).
import PlaceholderPage from "./PlaceholderPage";

export default function SignupPage() {
  return <PlaceholderPage title="Sign up (age gate)" owner="PR F3" />;
}
