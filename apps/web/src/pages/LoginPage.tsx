// Route stub only -- do not build the Login screen here.
// PR F2 replaces this file's contents with the real, Figma-derived screen.
import PlaceholderPage from "./PlaceholderPage";

export default function LoginPage() {
  return <PlaceholderPage title="Login" owner="PR F2" />;
}
